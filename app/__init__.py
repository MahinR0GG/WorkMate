"""
Application package for the HR Bot API.

Making this directory a package allows imports such as
```
from app.routes import chat
```
when the project root is on `sys.path` (e.g. using ``python -m app.main``
or ``python run.py``).
"""

# package-level initialization can go here if needed
