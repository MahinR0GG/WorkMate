# HR-Bot Requirements
# Python 3.10+ recommended

# ============================================
# Core Document Processing
# ============================================
python-docx>=0.8.11              # .docx file parsing for HR policy documents

# ============================================
# Vector Database & Embeddings
# ============================================
sentence-transformers>=2.2.0     # Embedding model (all-MiniLM-L6-v2)
faiss-cpu>=1.7.0                 # Fast similarity search (FAISS index)
numpy>=1.24.0                    # Numerical ops required by FAISS & embeddings

# ============================================
# LLM Orchestration - LangChain
# ============================================
langchain>=0.3.0                 # Core LangChain framework
langchain-core>=0.3.0            # LangChain base abstractions (LCEL, prompts, etc.)
langchain-ollama>=0.2.0          # ChatOllama wrapper for local Ollama models
tiktoken>=0.7.0                  # BPE token counter for STM trimming (provider-agnostic)

# Phase 2 (Azure OpenAI) — install when ready:
# langchain-openai>=0.2.0        # AzureChatOpenAI wrapper

# ============================================
# API & Web Server
# ============================================
fastapi>=0.104.0                 # REST API framework
uvicorn>=0.24.0                  # ASGI server for FastAPI
pydantic>=2.0.0                  # Request/response data validation

# ============================================
# Frontend
# ============================================
streamlit>=1.28.0                # Streamlit chat UI
requests>=2.31.0                 # HTTP client used by Streamlit to call API

# ============================================
# Utilities
# ============================================
python-dotenv>=1.0.0             # .env file loading for config
